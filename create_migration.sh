#!/usr/bin/env bash

mkdir -p accounts/migrations && touch accounts/migrations/__init__.py
mkdir -p datetimedim/migrations && touch datetimedim/migrations/__init__.py
mkdir -p profiles/migrations && touch profiles/migrations/__init__.py
mkdir -p locations/migrations && touch locations/migrations/__init__.py
mkdir -p payment_histories/migrations && touch payment_histories/migrations/__init__.py
mkdir -p users/migrations && touch users/migrations/__init__.py
mkdir -p loans/migrations && touch loans/migrations/__init__.py
